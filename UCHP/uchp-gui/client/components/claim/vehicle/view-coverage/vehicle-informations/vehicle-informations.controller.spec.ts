/*
'use strict';

describe('Component: VehicleInformationsComponent', function () {

  // load the controller's module
  beforeEach(module('uchpClientAngularApp'));

  var VehicleInformationsComponent, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($componentController, $rootScope) {
    scope = $rootScope.$new();
    VehicleInformationsComponent = $componentController('VehicleInformationsComponent', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
*/
